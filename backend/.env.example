# MongoDB connection string (from MongoDB Atlas or any provider)
MONGO_URI=mongodb+srv://<username>:<password>@cluster.xxxxx.mongodb.net/halleyx

# Your Vercel frontend URL (set after first deploy)
CLIENT_URL=https://your-project.vercel.app

# Keep as production on Vercel
NODE_ENV=production
